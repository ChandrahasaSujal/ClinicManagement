@model CM.Data.ViewModels.Medicine.CategoryViewModel
<div class="modal" data-keyboard="false" data-backdrop="static" id="addCategory" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="card-title">Category Details</h4>
                <button class="mdi mdi-close" data-dismiss="modal" />
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("Add", "Category", new { Area = "Admin" }, FormMethod.Post, new { id = "categoryForm" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(model => model.Id, new { id = "categoryId" })
                    <div class="form-group">
                        @Html.LabelFor(model => model.CategoryName, new { @class = "control-label" })
                        @Html.EditorFor(model => model.CategoryName, new { htmlAttributes = new { @class = "form-control"} })
                        @Html.ValidationMessageFor(model => model.CategoryName, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(model => model.CategoryDescription, new { @class = "control-label" })
                        @Html.TextAreaFor(model => model.CategoryDescription, new { @class = "form-control"} )
                    </div>
                }
            </div>
            <div class="modal-footer">
                <input type="submit" id="appointmentButton" onclick="addCategory()" class="btn btn-gradient-primary btn-rounded btn-fw" value="Add">
                <input type="reset" onclick="resetCategoryForm()" class="btn btn-gradient-secondary btn-rounded btn-fw" value="Reset">
            </div>
        </div>
    </div>
</div>